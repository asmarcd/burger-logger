<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-iseaten="{{eaten}}">
		{{#if eaten}}Order another!{{else}}Devour it!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Send it back!</button>
</li>